# GenSRT — base runtime dependencies.
#
# CUDA-specific PyTorch lives in requirements-cuda.txt and must be installed
# FIRST so pip doesn't pull the CPU build of torch from PyPI:
#
#   pip install -r requirements-cuda.txt
#   pip install -r requirements.txt
#   pip install -e .

faster-whisper>=1.0.3
requests>=2.31.0
transformers>=4.40.0
accelerate>=0.30.0
ffmpeg-python>=0.2.0
srt>=3.5.3
flask
pywebview
tqdm
rich>=13.0.0
